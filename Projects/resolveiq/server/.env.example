cat > .env.example <<'EOF'
NODE_ENV=development
PORT=3000

# Temporary development login.
# Replaced by database-backed authentication in a later slice.
DEV_LOGIN_EMAIL=admin@resolveiq.local
DEV_LOGIN_PASSWORD=ResolveIQ2026!
EOF